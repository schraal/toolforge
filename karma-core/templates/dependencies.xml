<?xml version="1.0"?>

<!-- dependencies file, modelled a bit like the maven project.xml, but without all the 'overhead' -->

<dependencies>

  <!-- Resolved as ~/.maven/repository/<groupId>/jars/<artifactId>-<version>.jar -->

  <dependency groupId="" artifactId="" version=""/>

  <!-- Resolved as ~/.maven/repository/<id>/jars/<jar> -->

  <dependency id="" jar=""/>

</dependencies>