JARS=`ls lib/*`
echo $JARS
CLASSPATH="resources:target/classes"
for JAR in $JARS ; do
  echo $JAR
  CLASSPATH=$CLASSPATH:$JAR
done

$JAVA_HOME/bin/java -cp $CLASSPATH -Dbootstrap.configuration=$HOME/.karma/karma.properties -Druntime.mode=COMMAND_LINE nl.toolforge.karma.cli.CLI $@

